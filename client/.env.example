# Copy to client/.env for local dev (usually not needed — the Vite dev
# server proxies /api to localhost:4000 by default, see vite.config.js).
# Required in production: point this at the deployed backend's URL.
VITE_API_BASE_URL=
